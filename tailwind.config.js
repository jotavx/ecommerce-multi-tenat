/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class", // Usamos la clase para activar el modo oscuro
  content: ["./src/**/*.{html,ts}"],
  theme: {
    extend: {},
  },
  plugins: [],
};
